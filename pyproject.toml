[project]
name = "runcoach"
version = "0.1.0"
description = "AI half-marathon coaching agent"
requires-python = ">=3.13"
dependencies = [
    "python-telegram-bot[job-queue]>=21.0",
    "garth>=0.4.0",
    "stravalib>=2.0",
    "pyyaml>=6.0",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
